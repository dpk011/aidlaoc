---
title: "Posts"
# Posts are listed on the News page, so this folder doesn't need a page of its
# own. `render: never` stops Hugo building one — matching the old site, which
# had no /posts/ address either.
build:
  render: never
  list: local
---
